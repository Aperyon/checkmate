import React from 'react';


export function HeroTitle({ children }) {
  return (<h1 className="HeroTitle">{children}</h1>)
}

export function HeroSubtitle({ children }) {
  return (<p className="HeroSubtitle">{children}</p>)
}

export function Subheader({ children }) {
  return (<h3 className="Subheader">{children}</h3>)
}

export function Text({ children }) {
  return (<p className="HeroSubtitle">{children}</p>)
}